object Shopping extends App {


  val map = Map("Apples" -> 0.60, "Oranges" -> 0.25)

  val shopping = List("Apple", "Orange", "Apple")


  for (i <- 0 until shopping.length){
    if (shopping(i)== map("Apples")){

    }
  }
  for (i <- 0 until shopping.length){
    if (shopping(i)==map("Oranges")){
    }
  }
  }
